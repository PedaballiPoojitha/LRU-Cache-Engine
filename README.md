# LRU-Cache-Engine
Implemented an **LRU (Least Recently Used) Cache Engine** using Python with O(1) time complexity for get and put operations using HashMap and Doubly Linked List. Efficiently manages fixed-capacity storage by evicting the least recently accessed item when the cache is full.
